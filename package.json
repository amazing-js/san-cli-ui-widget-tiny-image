{
  "name": "san-cli-ui-widget-tiny-image",
  "version": "0.0.1",
  "description": "san cli ui widgets tiny image",
  "scripts": {
    "prebuild": "rm -rf dist",
    "start": "cross-env NODE_ENV=development san serve --config san.config.js",
    "build": "cross-env NODE_ENV=production san build --config san.config.js",
    "prepublishOnly": "npm run build"
  },
  "keywords": [
    "San CLI UI",
    "San CLI WIDGETS",
    "CLI UI WIDGET"
  ],
  "author": "zttonly <zttonly@gmail.com>",
  "license": "MIT",
  "devDependencies": {
    "cross-env": "^7.0.2",
    "graphql-tag": "^2.11.0",
    "san-cli": "^0.1.5",
    "san-cli-ui": "^0.1.2"
  },
  "dependencies": {},
  "browserslist": [
    "> 1%",
    "last 2 versions",
    "not dead"
  ]
}
